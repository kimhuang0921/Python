{

    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug mainpack",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/mainpack.py",
            "console": "integratedTerminal",  // ✅ 這行很重要
            "args": [
                "--input", "0519_autogen.csv",
                "--xlsx-only"
            ]
        }
    ]

}